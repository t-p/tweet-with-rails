%div{ :id => "sidebar", :class => "span-8 round last"}
  %ul
    %li local_trends
    %li ToDo: error 403 handling
    - client.local_trends(23424829).each do |key|
      %li
        - if /^\#/ =~ key
          %a(href="/trend/#{key[1..-1]}")
            ="#{key}"
        - else
          %a(href="/trend/#{key}")
            ="#{key}"
